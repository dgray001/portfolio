import {html, css, LitElement} from 'lit';
import {customElement} from 'lit/decorators.js';

@customElement('dwg-fiddlesticks')
export class DwgLink extends LitElement {
  static styles = css`
  `;

  render() {
    return html`
    `;
  }
}